import { ReactNode } from 'react';
export default function If({ condition, children, suspense, fallback, }: {
    condition?: any;
    children?: ReactNode;
    suspense?: boolean;
    fallback?: ReactNode;
}): JSX.Element | null;
//# sourceMappingURL=If.d.ts.map